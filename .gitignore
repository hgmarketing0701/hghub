# scratch / unrelated working dir (94MB of PDF-processing + node_modules)
_pdfwork/

# dependencies
node_modules/

# local machine-specific Claude settings
.claude/settings.local.json

# local Supabase CLI link metadata
supabase/.temp/

# OS junk
Thumbs.db
.DS_Store

# ============ cPanel migration — NEVER commit these ============
# API secrets
server/.env
server/.env.*
# generated data dumps (contain production data incl. worker bank details)
import/out/
mysql/data-*.sql
mysql/*-data.sql
# raw production exports must never live inside the repo
migration-package/
sheets-data/
*.xlsx

# generated frontend bundle (run prepare-public.js on deploy)
server/public/
# deploy bundle DB (production data)
deploy/*.sql
deploy/
# backbone merge outputs (contain production names)
backbone/merge-*.csv
backbone/merge-state.json
backbone/apply-backbone.sql
